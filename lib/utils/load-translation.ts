import type { Exercise } from '@/types';

/**
 * Load translation formulas for exercise substitutions.
 * Thin stubs — implement during feature development.
 */

export function translateLoad(
  _from: Exercise,
  _to: Exercise,
  _currentWeight: number
): number {
  throw new Error('Not implemented');
}
